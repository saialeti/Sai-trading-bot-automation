apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-server-config
  namespace: default
  labels:
    app: webhook-server
    version: v1
data:
  PORT: "3001"
  NODE_ENV: "production"
  LOG_LEVEL: "info"
